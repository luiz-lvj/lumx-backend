 create table events(
        name text,
        description text,
        date integer,
        reward_per_sell integer);

create table influencers(
 name text,
 email text,
 wallet_id text,
 wallet_address text,
 referral_code text);
